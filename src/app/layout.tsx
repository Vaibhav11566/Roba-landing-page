import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Roba.ai — Building AI-powered tools for the modern workforce",
  description:
    "Roba.ai builds Gen AI products that solve real workforce problems. Explore Taskly, our AI-powered freelancer OS, and upcoming products.",
  openGraph: {
    title: "Roba.ai — Building AI-powered tools for the modern workforce",
    description:
      "Gen AI products for the modern workforce. Taskly, CareerCraft, and more.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      suppressHydrationWarning
      className={`${geistSans.variable} ${geistMono.variable}`}
    >
      <body className="min-h-screen antialiased">
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
